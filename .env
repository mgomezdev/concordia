HOST_PORT=8001
ORDINUS_PORT=3002

# Break-glass full-scope key for local dev / e2e test seeding (Themis auth).
# Fixed dev-only value, not a real secret — this stack is never internet-exposed.
THEMIS_BOOTSTRAP_KEY=concordia-local-e2e-bootstrap-key
