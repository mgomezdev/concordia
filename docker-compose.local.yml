# Build from local source instead of pulling from Docker Hub.
# Requires sibling repos ../laminus, ../themis, and ../ordinus to be present.
#
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up

services:
  laminus:
    build: ../laminus
    volumes:
      - ../laminus/config:/config
      - ../laminus/data:/data

  themis:
    build: ../themis

  ordinus:
    build:
      context: ../ordinus
      dockerfile: infra/Dockerfile
